type FormInput = {
  input: string;
  name: string;
  label?: string;
  placeholder?: string;
};

type Outcome = {
  icon?: string;
  title: string;
  subTitle?: string;
  description?: string;
  form?: any;
};

